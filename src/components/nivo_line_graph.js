import React from 'react';
import _ from 'lodash';
import { ResponsiveLine } from '@nivo/line';

export default class NivoLineGraph extends React.Component{
  render(){
    const { data } = this.props;

    return <div style={{
      height:0,
    }}>

      
    </div>;
  }
}